get:
  summary: List daily log tasks
  tags: [Daily Log Tasks]
  security: [{BearerAuth: []}]
  parameters:
    - name: limit
      in: query
      schema: {type: integer, minimum: 1, maximum: 100, default: 20}
    - name: cursor
      in: query
      schema: {type: string}
  responses:
    '200':
      description: List of daily log tasks
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ListResponse'
    '401': {$ref: '#/components/responses/Unauthorized'}
    '403': {$ref: '#/components/responses/Forbidden'}
    '500': {$ref: '#/components/responses/ServerError'}

post:
  summary: Create daily log task
  tags: [Daily Log Tasks]
  security: [{BearerAuth: []}]
  requestBody:
    required: true
    content:
      application/json:
        schema: {$ref: '#/components/schemas/CreateDailyLogTaskRequest'}
  responses:
    '201':
      description: Daily log task created
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SingleItemResponse'
    '401': {$ref: '#/components/responses/Unauthorized'}
    '403': {$ref: '#/components/responses/Forbidden'}
    '422': {$ref: '#/components/responses/ValidationError'}
    '500': {$ref: '#/components/responses/ServerError'}
